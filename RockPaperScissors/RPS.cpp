#include <iostream>
#include <vector>
#include <cmath>
#include <cstdlib>
#include <stdlib.h>
#include <time.h>
#include "storeData.h"
#include "startegy1.h"

using namespace std;

int main(){

    //srand(time(NULL));
    int length;
    cout << "Enter how many times you would like to run the program" << endl;
    cin >> length;
    strdata o1(length);
    
}